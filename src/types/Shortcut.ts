// SHORTCUT  - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
// the Shortcut type is used to describe a keyboard shortcut as defined in the
// keyboard store
export type Shortcut = {
  keys: string[]
  description: string
}
export default Shortcut
