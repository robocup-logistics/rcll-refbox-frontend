// RANDOMIZEFIELDOUTMSG  - - - - - - - - - - - - - - - - - - - - - - - - - - - -
// we send this message if we want to create another random field layout
export default interface RandomizeFieldOutMsg {
  command: 'randomize_field'
}
